# Keycloak configuration placeholder for future use
# Uncomment and fill in the values below to enable Keycloak integration

KEYCLOAK_CONFIG = {
    # 'SERVER_URL': 'https://your-keycloak-server/auth',
    # 'REALM': 'your-realm',
    # 'CLIENT_ID': 'your-client-id',
    # 'CLIENT_SECRET': 'your-client-secret',
    # 'VERIFY_SSL': True,
}

# To enable Keycloak authentication, install the required packages and update your authentication backends accordingly.
# Example packages: django-keycloak-auth, python-keycloak
# Example usage:
# AUTHENTICATION_BACKENDS = [
#     'keycloak_auth.backends.KeycloakAuthorizationCodeBackend',
#     'django.contrib.auth.backends.ModelBackend',
# ]
