// Keycloak configuration placeholder for future use
// Fill in these values and import where needed to enable Keycloak integration

export const keycloakConfig = {
  // url: 'https://your-keycloak-server/auth',
  // realm: 'your-realm',
  // clientId: 'your-client-id',
  // onLoad: 'login-required',
  // checkLoginIframe: false,
};

// To enable Keycloak, install the keycloak-js package:
// npm install keycloak-js
// Then use this config with the Keycloak JS adapter in your auth logic.
